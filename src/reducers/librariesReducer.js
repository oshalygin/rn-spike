/* eslint-disable indent */
import initialState from './initialState';

export default function usersReducer(state = initialState.libraries, action) {
  switch (action.type) {
    default: {
      return state;
    }
  }
}
